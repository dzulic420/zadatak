var assert = require('assert');
var chai = require("chai");
var expect = chai.expect;
var should = chai.should;
const suggestionTool = require("../suggestions");

describe('Pretrazivanje', function() {
    it('Searching', function () {
        suggestionTool();
    });
})